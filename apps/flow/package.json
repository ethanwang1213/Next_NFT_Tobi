{
  "name": "flow",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "emulator": "flow emulator",
    "deploy": "flow project deploy --network=emulator",
    "mint": "flow transactions send ./cadence/transactions/mintNFT.cdc --network=emulator 'metaURI'",
    "deploy-testnet": "flow project deploy --network=testnet",
    "test": "flow project test --network=emulator"
  },
  "dependencies": {
    "@onflow/fcl": "^1.3.2"
  }
}
